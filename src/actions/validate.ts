import fs from "fs";
import path from "path";
import { getRootHistory, findLauncherId } from "../blockchain/datastore";
import { DIG_FOLDER_PATH, getManifestFilePath } from "../config";
import zlib from "zlib";
import crypto from "crypto";



export const validate = async (): Promise<void> => {
  
};

